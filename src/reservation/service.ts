import Promise = require("bluebird");
import {getError, getResult, Result} from "../global/result";
import {encrypt, isNullOrUndefined} from "../global/utils";
import {IReservationRepository} from "./repository";


export interface IReservationService {

}

export class ReservationService implements IReservationRepository {
    
}